@model FootballWeb.Models.Team

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this team: @Model.Name?</h3>
<h4> Meciurile urmatoare vor fi sterse:  </h4>
<ul>
    @foreach (var m in Model.MatchIdTeamAwayNavigation) {
        <li> Meciul @m.Id </li>
    }

    @foreach (var m in Model.MatchIdTeamAwayNavigation)
    {
        <li> Meciul @m.Id </li>
    }
    Aceste meciuri vor fi sterse!
</ul>
<div>
    <h4>Team</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.ChampionshipPoints)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ChampionshipPoints)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.GoalsScored)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.GoalsScored)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
